api.array :places do
  @places.each do |place|
    api.place do
      api.id place.id
      api.name place.name
      api.category place.category
      api.point_x place.point_x
      api.point_y place.point_y
    end
  end
end
